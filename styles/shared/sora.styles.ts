import { StyleSheet } from 'react-native';

export const soraStyles = StyleSheet.create({
  halo: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  haloShadow: {
    shadowColor: '#5B9BC8',
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.25,
    shadowRadius: 10,
    elevation: 4,
  },
});
